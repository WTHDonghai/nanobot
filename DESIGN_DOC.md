# OpenViking 技术架构设计文档

> **版本**: 1.0  
> **用途**: 技术分享  
> **目标读者**: 后端工程师、AI工程师、架构师

---

## 1. 项目概述

### 1.1 定位

**OpenViking** 是 AI Agent 的 **Context Database（上下文数据库）**。

核心理念：**用文件系统范式统一 Agent 所需的三类上下文**

| 上下文类型 | 说明 | 示例 |
|-----------|------|------|
| 📁 **Resources** | 资源/文档 | 产品手册、API文档、合同模板 |
| 🧠 **Memories** | 记忆 | 会话历史、任务执行记录 |
| 🛠️ **Skills** | 技能 | 工具定义、Prompt模板、工作流 |

### 1.2 解决的核心痛点

```
┌─────────────────────────────────────────────────────────────┐
│  痛点1: 上下文碎片化                                         │
│  ├─ 记忆散落在代码中                                         │
│  ├─ 资源存储在向量数据库                                     │
│  └─ 技能定义各处分散                                         │
├─────────────────────────────────────────────────────────────┤
│  痛点2: 上下文需求激增                                       │
│  ├─ 长任务产生大量上下文                                     │
│  └─ 简单截断导致信息丢失                                     │
├─────────────────────────────────────────────────────────────┤
│  痛点3: 检索效果差                                           │
│  └─ 传统RAG扁平存储，缺乏全局视图                             │
├─────────────────────────────────────────────────────────────┤
│  痛点4: 检索不可观测                                         │
│  └─ 检索链是黑盒，难以调试                                   │
├─────────────────────────────────────────────────────────────┤
│  痛点5: 记忆无法迭代                                         │
│  └─ 当前记忆只是交互记录，缺乏任务相关记忆                     │
└─────────────────────────────────────────────────────────────┘
```

### 1.3 核心创新

**类比**: 像管理本地文件一样构建 Agent 的大脑

| 维度 | 传统RAG | OpenViking |
|------|---------|------------|
| **存储模型** | 平面向量存储 | 层级文件系统 |
| **检索方式** | 纯语义相似度 | 目录定位 + 语义搜索 |
| **上下文组织** | 分块存储 | L0/L1/L2三级结构 |
| **可观测性** | 黑盒 | 可视化检索轨迹 |
| **记忆迭代** | 无 | 自动压缩提取长期记忆 |

---

## 2. 系统架构

### 2.1 分层架构图

```
┌──────────────────────────────────────────────────────────────┐
│  Layer 1: Agent Layer                                        │
│  ┌──────────────┬──────────────┬──────────────────┐         │
│  │ 🤖 VikingBot │ 👥 客服机器人 │ 📋 投标助手      │         │
│  └──────────────┴──────────────┴──────────────────┘         │
└──────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────┐
│  Layer 2: OpenViking Server                                  │
│  ┌────────┬────────┬──────────┬─────────┬──────────┐        │
│  │ Parser │ Index  │ Retrieve │ VikingFS│ Session  │        │
│  │ (解析) │ (索引) │ (检索)   │ (文件)  │ (会话)   │        │
│  └────────┴────────┴──────────┴─────────┴──────────┘        │
└──────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────┐
│  Layer 3: Storage Layer                                      │
│  ┌────────────┬────────────────┬──────────────────┐         │
│  │ 💾 AGFS    │ 📊 VectorDB    │ 🔗 Embedding     │         │
│  │ 文件存储   │ 向量索引       │ 向量化模型       │         │
│  └────────────┴────────────────┴──────────────────┘         │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 核心组件职责

| 组件 | 职责 | 关键技术 |
|------|------|----------|
| **Parser** | 文档解析与结构化 | pdfplumber、MinerU API |
| **Index** | 向量索引构建 | Dense + Sparse 混合索引 |
| **Retrieve** | 层级检索 | 递归目录搜索算法 |
| **VikingFS** | 文件系统抽象 | 类似 POSIX 的语义层 |
| **SemanticQueue** | 异步语义处理 | 任务队列 + 熔断机制 |
| **Session Manager** | 会话状态管理 | 长期记忆同步 |

---

## 3. 核心设计

### 3.1 L0/L1/L2 三级上下文结构

```
┌─────────────────────────────────────────────────────────────┐
│  L0 (Abstract)        │  .abstract.md                       │
│  ├─ 精简摘要          │  一句话描述核心内容                  │
│  └─ 快速定位          │  用于全局语义搜索                    │
├─────────────────────────────────────────────────────────────┤
│  L1 (Overview)        │  .overview.md                       │
│  ├─ 结构化摘要        │  关键要点、目录结构                  │
│  └─ 上下文导航        │  用于目录定位和摘要检索              │
├─────────────────────────────────────────────────────────────┤
│  L2 (Content)         │  {filename}.md                      │
│  ├─ 完整内容          │  原始文档完整内容                    │
│  └─ 按需加载          │  仅在需要时读取                      │
└─────────────────────────────────────────────────────────────┘
         ↓
【优势】按需加载机制 → 显著降低 Token 消耗
```

**生成方式**: LLM 自动生成 + 底层并发控制

### 3.2 文档解析系统

#### 3.2.1 支持的格式

| 格式 | 处理方式 | 备注 |
|------|----------|------|
| **PDF** | pdfplumber / MinerU API 双策略 | 优先使用 MinerU |
| **Markdown** | 原生支持 | 保留原始结构 |
| **代码文件** | 多语言解析 | Python/JS/Go/Java 等 |
| **音视频** | 生成文字摘要 | Whisper + LLM 摘要 |

#### 3.2.2 PDF 解析流程

```
PDF File
    ↓
Convert to Markdown
    ↓
Parse Structure
    ├── 书签/大纲提取 → 自动生成目录结构
    ├── 字体分析      → 智能识别标题层级
    ├── 表格提取      → Markdown 表格格式
    └── 图片提取      → 独立存储 + 占位符引用
    ↓
VikingFS 存储
```

### 3.3 语义处理流水线

```
┌────────────┐    ┌──────────┐    ┌──────────┐    ┌────────────────┐
│   文档上传  │───→│  Parser  │───→│ VikingFS │───→│ SemanticQueue  │
└────────────┘    └──────────┘    └──────────┘    └────────┬───────┘
                                                          ↓
                                              ┌─────────────────────┐
                                              │  SemanticProcessor  │
                                              │   (LLM批量处理)     │
                                              └──────────┬──────────┘
                                                         ↓
                              ┌──────────────────────────────────────────┐
                              │   Generate Abstract + Overview           │
                              │   ├─ L0: 一句话摘要                      │
                              │   └─ L1: 结构化概述                      │
                              └────────────────────┬─────────────────────┘
                                                   ↓
                                        ┌────────────────────┐
                                        │   Embedding Queue  │
                                        └──────────┬─────────┘
                                                   ↓
                                        ┌────────────────────┐
                                        │    向量化并存储     │
                                        │  Dense + Sparse    │
                                        └────────────────────┘
```

**关键设计**:
- ⚡ **异步队列处理**: 非阻塞上传，提升用户体验
- 🛡️ **熔断机制**: Circuit Breaker 防止级联故障
- 🔄 **增量更新**: 支持文档版本管理和增量索引

### 3.4 索引系统架构

#### 3.4.1 向量索引

**存储选项**:
- VikingDB (字节内部)
- RocksDB (嵌入式)
- Milvus / 云厂商向量数据库

**支持向量类型**:
- **密集向量 (Dense)**: [1024/3072 维] - 语义相似度
- **稀疏向量 (Sparse)**: {term: weight} - 关键词匹配

#### 3.4.2 索引字段

```python
{
    "uri": "resource://account/space/path",     # 资源标识符
    "level": 0|1|2,                              # L0/L1/L2 层级
    "context_type": "resource|memory|skill",    # 上下文类型
    "abstract": "语义摘要文本",                   # 用于向量检索
    "vector": [0.1, 0.2, ...],                   # 密集向量
    "sparse_vector": {"term1": 0.5, ...},       # 稀疏向量
    "active_count": 42                           # 使用频次统计
}
```

#### 3.4.3 多租户隔离

```
数据隔离策略: account_id + owner_space

示例 URI: resource://account_123/sales_space/product_manual
                      │           │            │
                      └─ 账户ID    └─ 空间隔离   └─ 资源路径
```

### 3.5 检索引擎: HierarchicalRetriever

#### 3.5.1 检索流程

```
1. Query Embedding (查询向量化)
            ↓
2. Global Vector Search (全局向量检索 TOP-K)
            ↓
3. Merge Starting Points (合并起始点)
            ↓
4. Recursive Search (递归目录检索)
   ├─ 优先检索父目录(L0/L1)
   ├─ 分数向下传播
   └─ Rerank精排（可选）
            ↓
5. Return L2 Content (返回原始内容)
```

#### 3.5.2 递归目录检索算法

```python
def recursive_search(query, starting_points):
    # 优先队列：按分数排序
    dir_queue = PriorityQueue()
    collected = {}  # URI → 候选结果
    visited = set()
    
    # 初始化
    for uri, score in starting_points:
        dir_queue.push((-score, uri))
    
    while dir_queue not empty:
        current_score, current_uri = dir_queue.pop()
        if current_uri in visited:
            continue
        visited.add(current_uri)
        
        # 检索子项
        children = vector_search(parent=current_uri)
        for child in children:
            # 分数传播：组合父节点和子节点分数
            final_score = (alpha * child_score + 
                          (1-alpha) * current_score)
            
            if child.level == 2:
                # 终端节点：收集结果
                collected[child.uri] = child
            else:
                # 非终端：继续递归
                dir_queue.push((-final_score, child.uri))
    
    return sort_by_score(collected)
```

#### 3.5.3 核心参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `SCORE_PROPAGATION_ALPHA` | 0.5 | 父子分数权重比例 |
| `MAX_CONVERGENCE_ROUNDS` | 3 | 最大收敛轮数 |

**分数传播公式**:
```
final_score = α × child_score + (1-α) × parent_score
```

---

## 4. Agent 框架集成

### 4.1 VikingBot 架构

```
                    ┌───────────────────────────────────┐
                    │          AgentLoop                │
                    │        (核心循环)                  │
                    └──────────────┬────────────────────┘
                                   │
        ┌──────────────────────────┼──────────────────────────┐
        │                          │                          │
        ↓                          ↓                          ↓
┌───────────────┐      ┌──────────────────┐      ┌──────────────────┐
│ Context       │      │ Memory           │      │ ToolRegistry     │
│ Builder       │      │ Store            │      │ (工具注册)        │
│ (上下文构建)   │      │ (记忆存储)        │      └────────┬─────────┘
└───────┬───────┘      └────────┬─────────┘               │
        │                       │                         │
        └───────────────────────┼─────────────────────────┘
                                ↓
                    ┌───────────────────────┐
                    │    LLM Provider       │
                    │    (统一接口)          │
                    │  OpenAI / Anthropic   │
                    │  / Volcengine         │
                    └───────────────────────┘
```

### 4.2 关键特性

| 特性 | 说明 |
|------|------|
| **意图路由** | Intent Router 根据用户输入路由到不同处理逻辑 |
| **子代理管理** | SubagentManager 管理多个子 Agent 协作 |
| **会话记忆同步** | 自动同步会话状态到 OpenViking |

---

## 5. 应用场景

### 5.1 场景1: 智能客服支持机器人

#### 场景描述
基于产品文档、FAQ、历史工单构建客服知识库

#### 数据流程

```
产品文档/PDF ──→ Parser ──→ VikingFS ──→ 向量索引
                                              ↑
用户提问 ─────────────────────────────────────┤
    │                                         ↓
    ↓                              ┌──────────────────┐
检索相关知识 ←── AgentLoop ←──────┤  生成回答        │
    │                              └────────┬─────────┘
    ↓                                       ↓
记录会话 ────→ 自动提取 ────→ 长期记忆
```

#### 核心能力

| 能力 | 说明 |
|------|------|
| 📖 文档问答 | 基于产品手册的精准问答 |
| 🔗 历史工单关联 | 相似问题推荐 |
| 💬 多轮对话记忆 | 支持上下文连贯的多轮交互 |
| 👤 人工接管 | Human Handoff 机制 |

### 5.2 场景2: 智能投标助手

#### 场景描述
基于企业资质、产品方案、案例库辅助编写投标文件

#### 专用工具

| 工具 | 功能 | 数据来源 |
|------|------|----------|
| **SearchCertificatesTool** | 搜索资质证书 | 资质证书、许可证、授权资料 |
| **SearchSolutionMaterialsTool** | 搜索方案资料 | 解决方案、产品参数、案例 |
| **CollectBidEvidenceTool** | 收集投标证据 | 可复用的投标证据包 |

#### 工作流程

```
1. 解析招标文件 ──→ 提取需求要点
         ↓
2. 检索相关资质 ──→ 匹配证书资料
         ↓
3. 检索方案案例 ──→ 获取支撑证据
         ↓
4. 生成投标文件 ──→ 结构化输出
```

### 5.3 扩展: 更多 Agent 应用

```
OpenViking (通用知识库层)
      │
      ├─ 接入层: MCP (Model Context Protocol)
      │         └─ 标准接口供任意 Agent 调用
      │
      ├─ 工具层: Tool Registry
      │         └─ 可插拔工具生态
      │
      └─ 应用层: Vertical Agents
                │
                ├─ 🤖 客服机器人
                ├─ 📋 投标助手
                ├─ ⚖️ 法务助手
                ├─ 💻 研发助手
                ├─ 👥 HR助手
                └─ ...
```

#### 扩展方式

1. **自定义 Tool**: 继承 Tool 基类
2. **自定义 Prompt**: 场景化提示词
3. **自定义数据源**: 接入企业知识库

---

## 6. 技术栈

### 6.1 语言与运行时

| 语言 | 用途 |
|------|------|
| **Python 3.10+** | 主服务 |
| **Rust** | CLI 工具 `ov_cli` |
| **Go** | AGFS 文件系统 |

### 6.2 核心依赖

| 类别 | 选型 |
|------|------|
| **向量数据库** | VikingDB / RocksDB / Milvus |
| **嵌入模型** | OpenAI / Volcengine / Jina 等 |
| **VLM模型** | GPT-4V / Claude / Doubao 等 |
| **重排序** | Cohere Rerank / BGE Rerank |

### 6.3 部署方式

| 方式 | 适用场景 |
|------|----------|
| **Docker Compose** | 推荐，快速部署 |
| **Kubernetes** | 生产环境 |
| **单机二进制** | 快速体验 |

---

## 7. 未来演进

### 7.1 短期 (3-6个月)

- ✅ 完善 MCP 协议支持，无缝对接更多 Agent 框架
- ✅ 增强多模态能力（图片、视频、音频深度理解）
- ✅ 优化检索性能（支持 10 亿级文档）

### 7.2 中期 (6-12个月)

- 🔮 知识图谱集成（实体关系抽取与推理）
- 🔮 联邦检索（跨组织知识库检索）
- 🔮 自适应索引（根据查询模式自动优化）

### 7.3 长期 (1年+)

- 🚀 多 Agent 协同（共享知识库，分工协作）
- 🚀 主动学习（Agent 主动补充知识盲区）
- 🚀 个性化记忆（用户级记忆隔离与共享）

---

## 8. 总结

### 8.1 核心价值

| 序号 | 价值点 | 说明 |
|------|--------|------|
| 1 | **统一范式** | 用文件系统思维解决上下文管理难题 |
| 2 | **三级结构** | L0/L1/L2 按需加载，平衡精度与成本 |
| 3 | **递归检索** | 目录定位 + 语义搜索，提升检索效果 |
| 4 | **可观测性** | 可视化检索轨迹，白盒化调试 |
| 5 | **开放生态** | MCP 协议 + Tool Registry，支持任意 Agent 框架 |

### 8.2 愿景

> **成为 AI 时代的文件系统，让 Agent 拥有真正可用的长期记忆。**

---

## 附录

### A. 项目结构

```
openviking/
├── parse/              # 文档解析
│   └── parsers/        # 各格式解析器
├── index/              # 索引构建
├── retrieve/           # 检索引擎
├── vikingfs/           # 文件系统抽象
├── semantic/           # 语义处理队列
├── agent/              # Agent框架
│   ├── loop.py         # AgentLoop
│   ├── tools/          # 工具注册
│   └── context.py      # 上下文管理
└── server/             # 服务入口
```

### B. 关键配置

```yaml
# config.yaml 示例
server:
  host: 0.0.0.0
  port: 8080

storage:
  type: rocksdb  # rocksdb | vikingdb | milvus
  path: /data/openviking

embedding:
  provider: openai
  model: text-embedding-3-large
  dimension: 3072

retrieval:
  score_alpha: 0.5
  max_rounds: 3
```

### C. 相关资源

- PPT 演示文稿: `OpenViking_Premium.pptx`
- 项目文档: 详见各模块 README

---

**文档结束**
